spring:
  cache:
    type: simple
  mail:
    host: smtp.gmail.com
    port: 587
    username: notifications.finflow.in@gmail.com
    password: ghqiwvoaguejokts
    from: ${SPRING_MAIL_FROM:Finflow Alerts <notifications.finflow.in@gmail.com>}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
  rabbitmq:
    host: ${SPRING_RABBITMQ_HOST:localhost}
    port: 5672
  data:
    redis:
      host: ${SPRING_REDIS_HOST:localhost}
      port: 6379
  cloud:
    gateway:
      observability:
        enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:localhost}:8761/eureka/

management:
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: "http://${ZIPKIN_HOST:localhost}:9411/api/v2/spans"
  loki:
    url: "http://${LOKI_HOST:localhost}:3100/loki/api/v1/push"
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  pattern:
    level: "%5p [${spring.application.name:},%X{traceId:-},%X{spanId:-}]"